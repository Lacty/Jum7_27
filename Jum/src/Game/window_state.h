#pragma once

enum WindowSize {
  Width = 1024,
  Height = 600
};

enum {
  GROUND_Y = -280,
  EDGE_LEFT = -Width / 2,
  EDGE_RIGHT = Width / 2,
  EDGE_UP = Height / 2,
  EDGE_DOWN = -Height / 2
};

